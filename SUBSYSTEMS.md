# Subsystems

Each folder under `services/` and `apps/` is a deployable subsystem (one container image).

## Services
- canonical-api (public edge)
- contract-registry (internal)
- enforcement (internal)
- observation (internal)
- lineage (internal)

## Apps
- explorer-ui (public)
- control-plane-ui (public)
