apiVersion: v1
kind: Service
metadata: { name: canonical-api, namespace: nvariant }
spec:
  type: ClusterIP
  selector: { app: canonical-api }
  ports: [{ port: 80, targetPort: 3000 }]
---
apiVersion: v1
kind: Service
metadata: { name: explorer-ui, namespace: nvariant }
spec:
  type: ClusterIP
  selector: { app: explorer-ui }
  ports: [{ port: 80, targetPort: 80 }]
---
apiVersion: v1
kind: Service
metadata: { name: control-plane-ui, namespace: nvariant }
spec:
  type: ClusterIP
  selector: { app: control-plane-ui }
  ports: [{ port: 80, targetPort: 80 }]
---
apiVersion: v1
kind: Service
metadata: { name: contract-registry, namespace: nvariant }
spec:
  type: ClusterIP
  selector: { app: contract-registry }
  ports: [{ port: 7001, targetPort: 7001 }]
---
apiVersion: v1
kind: Service
metadata: { name: enforcement, namespace: nvariant }
spec:
  type: ClusterIP
  selector: { app: enforcement }
  ports: [{ port: 7004, targetPort: 7004 }]
---
apiVersion: v1
kind: Service
metadata: { name: observation, namespace: nvariant }
spec:
  type: ClusterIP
  selector: { app: observation }
  ports: [{ port: 7008, targetPort: 7008 }]
---
apiVersion: v1
kind: Service
metadata: { name: lineage, namespace: nvariant }
spec:
  type: ClusterIP
  selector: { app: lineage }
  ports: [{ port: 7006, targetPort: 7006 }]
